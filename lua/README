This directory has Lua-based Torch stuff.

How to install dependencies

1. Install Torch
See the install instructions at:
http://torch.ch/docs/getting-started.html#_

2. Install ZeroMQ
brew install zeromq
brew link zeromq

3. Install python parts
pip install ipython
pip install pyzmq
pip install jinja2
pip install tornado

5. Install iTorch
See the install instructions at:
https://github.com/facebook/iTorch
