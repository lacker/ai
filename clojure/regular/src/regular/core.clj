(ns regular.core
  (:gen-class))

; Tools for finding regular expressions.

; TODO: make this work
(defn find-all [in out]
  "Lazily lists all regular expressions that map in to out."
  [])

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))
